[build]
target = "index.html"
dist = "dist"
release = true
filehash = true

# Disable wasm-opt completely - its validator doesn't support bulk-memory
# which Rust uses by default. Modern browsers handle this fine.
[tools]
wasm_opt = "off"
